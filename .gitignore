# Delphi reference material — not a deliverable, large binary artifacts
XRD Converter Source/

# macOS zip metadata
__MACOSX/
.DS_Store

# Windows / editor clutter
Thumbs.db
desktop.ini
*.swp
*.swo
*~

# MATLAB artifacts
*.asv
*.m~
*.mex*
*.mat
slprj/

# Standalone build output (generated binaries — release artifacts, not source)
xrdc-matlab/build/standalone/

# Demo outputs (exportgraphics targets)
xrdc-matlab/*.png
xrdc-matlab/*.pdf
xrdc-matlab/*.eps
xrdc-matlab/*.fig
xrdc-matlab/examples/*.png
xrdc-matlab/examples/*.pdf
xrdc-matlab/examples/*.eps
xrdc-matlab/examples/*.fig

# XRD test data — Tushar's measurement scans are committed in-repo so
# collaborators can run the gated real-data tests without a separate download.
# Exclude bulky non-data artifacts: PDFs, archives (the legacy data.zip bundle
# and the email zip), and reference scripts.
data/*.pdf
data/*.zip
data/*.m
