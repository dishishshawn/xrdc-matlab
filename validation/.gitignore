# Researcher-supplied input scans are now committed so collaborators can run
# the gated validation tests without a separate download. Reference figures
# (his) and our exported outputs stay out of the repo.
tushar/reference/*
tushar/output/*

!**/.gitkeep
!**/README.md
!**/RESULTS.md
